# Python
__pycache__/
*.pyc

# Virtualenv
venv/
.venv/

# PyCharm
.idea/

# Archiwa
*.tar.gz
*.zip

# Claude
.claude/

# Projekt
psc2.purr
todo.txt

# Appdata — dane runtime (nie wersjonujemy)
appdata/model_ctx_registry.json.bak
appdata/logs/
appdata/rag/
appdata/api_keys.json
appdata/api_profiles.json
appdata/session.json
appdata/psnotes.txt
appdata/ob_panel_state.json
appdata/terminal_fifos/
appdata/chat_sessions/
appdata/venv_list.json
appdata/scripts_docs/
appdata/scripts_help/
appdata/scripts_history/
appdata/scripts_notes/
appdata/scripts_favorities/

# Cyb3rCollector — dane runtime (listeners, stagers, webmap scans, reports).
# Folder tworzony przy starcie przez controller, więc nie potrzeba .gitkeep.
appmodules/Cyb3rCollector/

# BrainDump — prywatne notatki i pliki RAG (nie wersjonujemy)
appmodules/BrainDump/*
!appmodules/BrainDump/user_guide.md

# AI Skills — klonowane na żądanie przez installer (nie wersjonujemy).
# Nie są to submoduły ani vendored — dzięki temu goły `git clone` jest czysty.
appdata/agent_modes/skills/

# Pliki audio
*.wav

# usermodules — pliki usera (nie wersjonujemy), tylko FIRST_STEPS.md
usermodules/*
!usermodules/FIRST_STEPS.md


# Old installer scripts
install_old.sh
install_full_old.sh

# Old / superseded reference files
requirements_old.txt
